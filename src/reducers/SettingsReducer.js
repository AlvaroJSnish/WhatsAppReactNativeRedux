const INITIAL_STATE = {
  name: "Alvaro",
  link: "https://github.com/AlvaroJSnish",
  battlecry: "For the watch",
  picture: "https://preview.ibb.co/c0z8ZT/descarga.png"
};

const SettingsReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default SettingsReducer;
